<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FF151C2E">

    <!-- Handle bar -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:background="#FF2A3550"
        android:layout_marginBottom="8dp"/>

    <!-- Header row: חץ חזרה + כותרת -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp">

        <ImageButton
            android:id="@+id/btnSearchBack"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/baseline_arrow_back_24"
            android:tint="#FFE6EDF3"
            android:contentDescription="Back"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Search Symbol"
            android:textColor="#FFE6EDF3"
            android:textSize="18sp"
            android:textStyle="bold"
            android:paddingStart="4dp"/>
    </LinearLayout>

    <!-- Search box -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:marginStart="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:paddingStart="14dp"
        android:paddingEnd="10dp"
        android:background="@drawable/bg_search_input">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔍"
            android:textSize="16sp"
            android:paddingEnd="10dp"/>

        <AutoCompleteTextView
            android:id="@+id/searchTickerInput"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:hint="e.g. AAPL, BTC, TSLA"
            android:hintTextColor="#FF4A5568"
            android:textColor="#FFE6EDF3"
            android:textSize="17sp"
            android:background="@null"
            android:inputType="textCapCharacters"
            android:singleLine="true"
            android:imeOptions="actionSearch"
            android:completionThreshold="1"/>

        <ImageButton
            android:id="@+id/btnClearSearch"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/baseline_close_24"
            android:tint="#FF8B98A5"
            android:contentDescription="Clear"
            android:visibility="gone"/>
    </LinearLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="14dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:background="#FF1E2A3A"/>

    <!-- Label "Results" -->
    <TextView
        android:id="@+id/labelResults"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Popular"
        android:textColor="#FF8B98A5"
        android:textSize="11sp"
        android:letterSpacing="0.08"
        android:paddingStart="20dp"
        android:paddingTop="14dp"
        android:paddingBottom="6dp"/>

    <!-- רשימת תוצאות -->
    <ListView
        android:id="@+id/searchResultsList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="#FF1E2A3A"
        android:dividerHeight="1dp"
        android:paddingTop="4dp"
        android:clipToPadding="false"
        android:scrollbarStyle="outsideOverlay"/>

</LinearLayout>